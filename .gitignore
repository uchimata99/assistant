# ארכיון מקומי, לא נדחף
assistant.zip

# פסולת של המערכת ושל הדרייב
desktop.ini
Thumbs.db
.DS_Store
*.tmp

# מזהה הפרויקט באפס סקריפט שונה בכל חשבון גוגל — לא נדחף
.clasp.json
apps_script/.clasp.json

apps_script/.clasp-*.json

# תצורת הרצה מקומית לבדיקות, לא חלק מהאפליקציה
.claude/
